apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: flask-dev

resources:
  - ../../base
  - namespace.yaml

generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: mysql-secret
    literals:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_PASSWORD=root
